# Primary navigation.
# NOTE: `name` must match the `navbar_title` in each page's front matter so the
#       active item is highlighted. URLs must match each page's `permalink`.
pages:
- name: Home
  url: /
- name: Publications
  url: /publications/
- name: Gallery
  url: /gallery/
- name: Notes
  url: /notes/
